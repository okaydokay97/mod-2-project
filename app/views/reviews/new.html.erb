
<h1> Create a New Review  </h1>
<%=form_for @review do |r|%>
  <p> A drop down box for showing movie title? to write a review?
  
  <br>


<%= form_for @review do |r| %>
  <%= r.label :content %>
  <%= r.text_field :content %><br>
  <%= r.label :rating %>
  <%= r.text_field :rating %><br>
  <%= r.submit %>
<% end %>